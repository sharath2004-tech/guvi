services:
  - type: web
    name: ai-voice-detection
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements_minimal.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: API_KEY
        value: sk_live_abc123xyz789_secure_key_2024